# Asteroids IA

Just train a model to get its data. Deliver as an AI class test.

## Install

```bash
python -m pip install -r requirements.txt
```

## Run

```bash
docker run --rm -it -v .:/app -w /app --net host python:3.12.12-alpine sh
```
